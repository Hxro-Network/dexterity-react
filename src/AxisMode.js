export default {
    Everything: 0,  // simply re-renders book from top to bottom with no logic regarding axis
    Midpoint: 1, // ensures the midpoint is always on the same level
    UserControlled: 2, // the axis can only move via user input
}
